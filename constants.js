const status = {
  ERROR_EXIT_STATUS: 1
};

const argIndexes = {
  OLD_SCHEMA_ARG_INDEX: 2,
  NEW_SCHEMA_ARG_INDEX: 3
};

const categories = {
  BREAKING: 'BREAKING'
};

module.exports = { status, argIndexes, categories };
